services:
  backend:
    build: backend
    env_file: backend/.env
    ports:
      - "8000:8000"
    develop:
      watch:
        - action: sync
          path: backend
          target: /app
        - action: rebuild
          path: backend/pyproject.toml
